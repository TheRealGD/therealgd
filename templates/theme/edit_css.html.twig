{% extends 'base.html.twig' %}

{% block title 'title.editing_theme'|trans({'%name%': theme.name}) %}
{% block page_classes 'edit-theme-css-page' %}

{% block body %}
  {{ include('theme/_nav.html.twig', {
    current_page: 'css',
    theme: theme,
  }, with_context=false) }}

  <h1 class="page-heading">{{ block('title') }}</h1>

  {{ include('theme/_css_form.html.twig', {form: form}, with_context=false) }}
{% endblock %}
